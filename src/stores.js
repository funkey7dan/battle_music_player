import { writable } from "svelte/store";

export const filelist_store = writable();
export const narrationFilelist_store = writable();
export const current_howl = writable();
export const current_narration_playlist = writable();
export const current_narration = writable();
export const valueNow = writable();
export const trackProgress = writable();
export const state = writable();
export const isSfx = writable();
